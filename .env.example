DATABASE_URL=postgresql+asyncpg://miya:miya@localhost:5432/miya
MEDIA_ROOT=./media
PUBLIC_BASE_URL=http://localhost:8000

# --- Auth ---
# iOS OAuth client id from Google Cloud Console (bundle id com.hurtado.Miya).
GOOGLE_IOS_CLIENT_ID=
# openssl rand -hex 32
JWT_SECRET=
ACCESS_TOKEN_TTL_SECONDS=900
REFRESH_TOKEN_TTL_DAYS=30
# openssl rand -hex 32  -- separate key so media links can be rotated
# without signing everyone out.
MEDIA_URL_SECRET=
